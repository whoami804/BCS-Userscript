name: Publish beta userscript

on:
  push:
    paths:
      - 'patches/rc13-to-rc14.patch.gz.b64'
      - '.github/workflows/publish-beta.yml'
  pull_request:
    paths:
      - 'patches/rc13-to-rc14.patch.gz.b64'
      - '.github/workflows/publish-beta.yml'

permissions:
  contents: write

jobs:
  publish:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref || github.ref_name }}

      - name: Patch and validate beta userscript
        shell: bash
        run: |
          echo '81ad091122482aa9f3622d5d6a0a4ca0eb3171033bca5fc5b27dbcafd5f26605  control-suite-boosteroid-beta.user.js' | sha256sum -c -
          base64 -d patches/rc13-to-rc14.patch.gz.b64 | gzip -d > /tmp/rc13-to-rc14.patch
          cp control-suite-boosteroid-beta.user.js /tmp/beta-next.user.js
          patch --batch /tmp/beta-next.user.js < /tmp/rc13-to-rc14.patch
          echo '3d58116015c211242d9962be95ac9610a43c197cb239537f73deedaae06d9410  /tmp/beta-next.user.js' | sha256sum -c -
          node --check /tmp/beta-next.user.js
          mv /tmp/beta-next.user.js control-suite-boosteroid-beta.user.js

      - name: Publish fixed beta URL
        shell: bash
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add control-suite-boosteroid-beta.user.js
          if git diff --cached --quiet; then
            echo 'No distribution change.'
            exit 0
          fi
          git commit -m 'Publish v0.8.1-rc14 beta userscript'
          git push origin HEAD:${{ github.head_ref || github.ref_name }}
